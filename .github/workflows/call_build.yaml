name: Test github action call with fixed boolean inputs

on:
  workflow_dispatch:
  push:

jobs:
  test:
    uses: radiomix/gha-bool-input-test./.github/workflows/build.yaml
    with:
      passed_true: true
      passed_false: false
